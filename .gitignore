# Изображения размещены на внешнем хостинге (postimages), в репозиторий не пушатся.
# Локальные оптимизированные оригиналы хранятся в img/ как резервная копия.
img/

# Исходники иллюстраций (локальные). Используемые на странице —
# railway-track-and-stop / train / birds — выносятся на внешний хостинг ссылками.
illustrations/

# macOS
.DS_Store
